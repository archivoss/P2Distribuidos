apply plugin: 'java'

repositories {
    mavenCentral()
}

dependencies {
    implementation fileTree(dir: 'lib', include: ['*.jar'])
}

task run(type: JavaExec) {
    main = 'ServidorChat' // Reemplaza con el nombre completo de tu clase principal
    classpath = sourceSets.main.runtimeClasspath
}